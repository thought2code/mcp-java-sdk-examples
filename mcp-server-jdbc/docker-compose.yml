services:
  mysql:
    image: mysql:8.4
    container_name: mcp-server-jdbc-mysql
    environment:
      MYSQL_USER: mcp
      MYSQL_PASSWORD: mcp
      MYSQL_DATABASE: mcp
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - ./mcp-server-jdbc-common/src/test/resources/init.sql:/docker-entrypoint-initdb.d/001-init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-umcp", "-pmcp"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 20s
